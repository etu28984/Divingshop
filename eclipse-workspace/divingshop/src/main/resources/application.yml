# Local server
server:
  # port is used by spring-boot-admin
  port: 8080
  contextPath: /divingshop
  
spring:
  datasource:
    password: password
    url: jdbc:mysql://localhost:3306/divingshopdb?user=root